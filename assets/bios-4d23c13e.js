import{p as t,b as d,C as o}from"./vue-vendor-de6b2611.js";const c={class:"markdown-body"},a=o("<h1>BIOS Settings</h1><h2>Basic Operations</h2><table><thead><tr><th>Item</th><th>R2</th><th>R2 Max</th></tr></thead><tbody><tr><td>BIOS menu</td><td><code>F2</code></td><td><code>Esc</code> or <code>F2</code></td></tr><tr><td>Boot order</td><td><code>F12</code></td><td><code>F11</code></td></tr><tr><td>Restore factory defaults</td><td><code>Save &amp; Exit</code> → <code>Restore Defaults</code></td><td><code>Save &amp; Exit</code> → <code>Restore Defaults</code></td></tr></tbody></table><h2>Power and Wake Settings</h2><table><thead><tr><th>Setting</th><th>R2</th><th>R2 Max</th></tr></thead><tbody><tr><td>Auto power on</td><td><code>Chipset</code> → <code>PCH-IO Configuration</code> → <code>State After G3</code>; select <code>S0 State</code> for power-on and <code>S5 State</code> for manual power control</td><td><code>Advanced</code> → <code>IT8613 Super IO Configuration</code> → <code>Restore AC Power Loss</code></td></tr><tr><td>USB power</td><td><code>Chipset</code> → <code>USB 5V Configuration</code> → <code>USB 5V ALWAY</code></td><td><code>Advanced</code> → <code>IT8613 Super IO Configuration</code> → <code>USB Power CTL</code></td></tr><tr><td>WOL and onboard LAN power</td><td><code>Chipset</code> → <code>PCIE LAN Configuration</code> → <code>Wake on LAN Enable</code></td><td><code>Advanced</code> → <code>ACPI Settings</code> → <code>Wake by onboard LAN</code></td></tr></tbody></table><h2>Fan Settings</h2><table><thead><tr><th>Setting</th><th>R2</th><th>R2 Max</th></tr></thead><tbody><tr><td>PWM fan</td><td><code>Advanced</code> → <code>Hardware Monitor</code> → <code>Smart Fan Function</code> → <code>CPU Fan Configuration</code>; available modes: <code>Normal Mode</code>, <code>Manual Mode</code>, <code>Full on Mode</code></td><td><code>Advanced</code> → <code>Hardware Monitor</code>; supported modes: <code>PWM Mode</code>, <code>Full on Speed</code>, <code>Fan Off Mode</code></td></tr></tbody></table>",7),r=[a],S={__name:"bios",setup(n,{expose:e}){return e({frontmatter:{}}),(h,i)=>(t(),d("div",c,r))}};export{S as default};
